@model Places
<h1>Add some places!</h1>
<form asp-action="AddPlaceAction" method="POST" id="AddPlaceAction">
    <label>Name</label>
    <input asp-for="Name" id="inputNameAddPlace">
    <label>Address</label>
    <input asp-for="Address" id="inputAddressAddPlace">
    <input type="hidden" asp-for="LatLong" id="LatLongEntered">
    <div class="dropdown">
      <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">Dropdown Example
      <span class="caret"></span></button>
      <ul class="dropdown-menu">
        @foreach (var item in Enum.GetValues(typeof(Enums.Categories)))
        {
          <li>
            <label>@item</label>
            <input asp-for="Category" type="radio" value=@item>
          </li>
        }
      </ul>
      <button disabled="disabled" type="button" onclick="AddressPartial()" data-toggle="modal" data-target="#addressesModal" id="openPopUp">
Agregar tarea
</button>
  </div>
   

</form>

<!--Modal Select Address-->
<div class="modal fade" id="addressesModal" tabindex="-1" role="dialog" aria-labelledby="addressesModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="addressesModalLabel">Seleccione la dirección correcta</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
    <div class="modal-body">
    </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">cerrar</button>
        <button type="button" class="btn btn-primary" id="addTodoBtn">Salvar cambios</button>
      </div>
    </div>
  </div>
</div>

